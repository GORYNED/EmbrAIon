# Validation

Validation is evidence that a framework change preserves intended contracts.

The validation system is expected to grow in layers:

- schema validation;
- capability-reference validation;
- adapter/projection parity;
- routing-policy validation;
- installation and doctor checks;
- integration tests.

A passing validation result should identify what was checked rather than merely report a generic success state.
