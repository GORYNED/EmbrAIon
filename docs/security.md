# Security

EmbrAIon treats execution permissions, data classification, provider eligibility, and mutation rights as enforceable engineering constraints.

Security policy belongs in canonical rules and routing. Provider or host adapters implement the corresponding mechanics.

The framework should fail closed when a task cannot be safely classified or when a provider is not permitted for the relevant data.
