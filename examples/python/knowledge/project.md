# Python Reference Knowledge

This is a public generic Python example used to prove that EmbrAIon can wrap a normal software repository without becoming an application dependency.

The application has no network calls and no external runtime dependencies.
