# Python Reference Architecture

- Product behavior lives under `src/reference_app/`.
- Product tests live under `tests/`.
- EmbrAIon project configuration lives under `.embraion/`.
- Project knowledge may describe application facts but must not duplicate canonical EmbrAIon Core policy.
- Generated host projections are disposable and are not canonical application source.
